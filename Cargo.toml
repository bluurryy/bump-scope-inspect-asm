[package]
name = "inspect-asm"
version = "0.1.0"
edition = "2021"

[dependencies]
bump-scope = { version = "0.16.5" }
bumpalo = { version = "3.17.0", features = ["allocator-api2"] }

# [patch.crates-io]
# bump-scope = { path = "../bump-scope" }
