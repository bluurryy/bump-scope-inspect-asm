[package]
name = "inspect-asm"
version = "0.1.0"
edition = "2021"

[dependencies]
bump-scope = { version = "0.16.0" }
bumpalo = { version = "3.16.0", features = ["allocator-api2"] }

# [patch.crates-io]
# noise-functions = { path = "../noise-functions" }
